#include <stdio.h>

int main()
{
    // intentional goof-up
    // writeln("Another Horrible Mistake!");
    // return(0);
    // header file for library not inmplemented in code to use writeln()

    // replace writeln()
    puts("Another Horrible Mistake! or is it?");
    return(0);
    
}